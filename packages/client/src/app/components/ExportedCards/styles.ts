import { addStylesheet } from "app/utilities/styles";

export const { classes } = addStylesheet({
  text: {
    overflowY: "scroll",
    flex: "1",
    whiteSpace: "pre",
    overflowWrap: "normal",
    overflowX: "scroll",
  },
});
