import { DRAWER_BACKGROND, addStylesheet } from "../../utilities/styles";

export const { classes } = addStylesheet((theme) => ({
  root: {
    background: DRAWER_BACKGROND,
    width: "200px",
  },
}));
